language: node_js
services:
node_js:
  - 0.10
before_install:
  - "sudo apt-get install libzmq3-dev"
before_script:
  - npm install -g grunt-cli
  - npm install -g codeclimate-test-reporter
  - npm install -d
branches:
  only:
    - master
